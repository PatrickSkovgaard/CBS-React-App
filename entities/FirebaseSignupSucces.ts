export class FirebaseSignupSucces{
    constructor (public idToken:String, public email: string, public refreshToken: string, public expiresIn: 
                string, public localId: string){
                    
        }
}